# Git LFS configuration for large model files
# Use Git LFS if you want to store models in the repository
# Model files (currently stored separately)
*.gguf filter=lfs diff=lfs merge=lfs -text
*.bin filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text
# Prevent accidental commits of large files
*.gguf diff=lfs merge=lfs -text
backend/models/*.gguf filter=lfs diff=lfs merge=lfs -text
